**THIS IS ONLY A ROUGH SKETCH**

First we notice that any union of $m$ sets have a cardinality of strictly larger than $m$. Otherwise this will form a cycle.

**THE FOLLOWING IS TO BE CLARIFIED AND UNDERSTOOD**

The condition is sort of like Hall's theorem. We can find a sort of bipartite matching so that we choose 1 element from every set to represent that set (**MAYBE FALSE AND IS THE OTHER WAY ROUND**)

Then we can do some sort of Ad-hoc algorithm to construct the rest.